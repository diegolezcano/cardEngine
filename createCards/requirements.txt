# Yu-Gi-Oh! Card Creator - Python Dependencies

# HTTP requests for downloading images
requests>=2.28.0

# Image processing (resize, convert formats)
# Optional but highly recommended for automatic image resizing
Pillow>=9.0.0

# Note: sqlite3 is included with Python standard library (no installation needed)

